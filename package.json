{
  "name": "nowforge",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter chrome-extension dev",
    "build": "pnpm --filter core build && pnpm --filter chrome-extension build && pnpm --filter @nowforge/vscode-extension build",
    "build:vscode": "pnpm --filter core build && pnpm --filter @nowforge/vscode-extension build",
    "lint": "eslint --ext .ts,.tsx packages/chrome-extension/src packages/core/src packages/types/src packages/vscode-extension/src packages/eslint-plugin-nowforge/src packages/cloud/src packages/mcp-server/src",
    "typecheck": "pnpm -r typecheck",
    "package": "pnpm build && node scripts/package.mjs"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  }
}

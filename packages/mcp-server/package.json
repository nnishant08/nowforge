{
  "name": "@nowforge/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for ServiceNow — connects Claude Desktop / Cursor / Windsurf / VS Code to your SN instance.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "nowforge-mcp": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@nowforge/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
